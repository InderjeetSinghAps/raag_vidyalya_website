---
active: true
iteration: 0
maxIterations: 50
sessionId: ses_whatsapp_debug
---

Debug the WhatsApp messaging system pipeline at a great level: trace the full data flow from API types → mapping → rendering → WhatsApp URL construction. Check every edge case, failure mode, and refinement opportunity. Check for chain reactions (side effects from our changes on other parts of the system). Keep iterating until the entire pipeline is fully analyzed, refined, and verified.
